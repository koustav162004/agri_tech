tensorflow==2.12.0  # Use TensorFlow 2.12 for older Python versions
numpy==1.23.5  # NumPy 2.0 requires Python 3.8+
opencv-python==4.9.0.80  # Latest stable OpenCV version for Python 3.6+
matplotlib==3.6.3  # Newer versions might not support older Python
flask==2.2.5  # Flask 3.0 requires Python 3.8+
werkzeug==2.2.3  # Compatible with Flask 2.2
gunicorn==21.2.0  # WSGI server for deploying Flask apps
